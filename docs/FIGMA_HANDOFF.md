# Figma handoff

The four-state design is already represented by code + QA screenshots:

- `test-artifacts/baselines/home.png`
- `test-artifacts/baselines/start-focus.png`
- `test-artifacts/baselines/focus.png`
- `test-artifacts/baselines/wrap.png`

A live editable Figma file was not created in this run because the connected Figma account exposes two different plans, and the Figma connector requires an explicit plan choice before file creation. Once a plan is chosen, these screenshots and the design tokens in `src/styles/tokens.css` are ready to import/rebuild as components.
